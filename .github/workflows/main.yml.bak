on:
  push:
  schedule:
    - cron: '0 8 * * 6'
    
jobs:
  Build:
    runs-on: ubuntu-latest
    steps: 
    - uses: actions/checkout@v2
    - name: Building
      shell: bash
      run: |
        echo Build
        bash Main.sh volteer ChromeOS.img